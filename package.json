{
  "name": "wan",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "wan": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile wan",
    "test": "bun test"
  }
}
